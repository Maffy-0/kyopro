#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using namespace atcoder;

void fast_io() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
}

signed main(void) {
    fast_io();
    int N, Q;
    string S;
    cin >> N >> Q >> S;
    for (int i = 0; i < N - 1; i++) {
    }
    while (Q--) {
        int op, l, r;
        cin >> op >> l >> r;
        l--;
        if (op == 1) {
        } else {
        }
    }
    return 0;
}