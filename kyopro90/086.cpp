#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using namespace atcoder;

void fast_io() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
}

using mint = modint1000000007;

signed main(void) {
    fast_io();
    int N, Q;
    cin >> N >> Q;
    while (Q--) {
        int x, y, z;
        long w;
        cin >> x >> y >> z >> w;
    }
    return 0;
}