#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using namespace atcoder;

void fast_io() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
}

signed main(void) {
    fast_io();
    long a, b, C;
    cin >> a >> b >> C;
    for (int i = 0; i < 60; i++) {
        if (C >> i & 1) {
        
        }
    }
    return 0;
}